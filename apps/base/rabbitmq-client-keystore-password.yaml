apiVersion: v1
kind: Secret
metadata:
  name: rabbitmq-client-keystore-password
  namespace: bookit
  annotations:
    # Workloads consume this Secret directly, so their Argo application owns it.
    argocd.argoproj.io/sync-wave: "-2"
type: Opaque
data:
  # PKCS#12 compatibility password; it is not a RabbitMQ login credential.
  password: Ym9va2l0LXBrY3MxMg==
