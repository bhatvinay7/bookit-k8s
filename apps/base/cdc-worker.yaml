apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: cdc-worker
  name: cdc-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cdc-worker
  template:
    metadata:
      labels:
        app: cdc-worker
    spec:
      containers:
        - envFrom:
            - secretRef:
                name: backend-secrets
            - configMapRef:
                name: bookit-config
          env:
            - name: OTEL_SERVICE_NAME
              value: bookit-cdc-worker
            - name: K8S_NAMESPACE
              valueFrom: { fieldRef: { fieldPath: metadata.namespace } }
            - name: POD_NAME
              valueFrom: { fieldRef: { fieldPath: metadata.name } }
            - name: OTEL_RESOURCE_ATTRIBUTES
              value: k8s.namespace.name=$(K8S_NAMESPACE),k8s.pod.name=$(POD_NAME)
          image: bookit-cdc-worker:latest
          name: cdc-worker
          resources:
            limits:
              cpu: 500m
              memory: 128Mi
            requests:
              cpu: 50m
              memory: 64Mi
      imagePullSecrets:
        - name: ghcr-secret
      serviceAccountName: bookit-app
