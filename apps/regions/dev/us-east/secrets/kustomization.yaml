apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# CI adds only SealedSecret ciphertext files here. An empty list keeps the
# overlay renderable during first bootstrap, before secrets are sealed.
resources: []
