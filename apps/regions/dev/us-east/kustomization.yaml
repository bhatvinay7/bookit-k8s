apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ../../../overlays/dev
- secrets

configMapGenerator:
- name: bookit-config
  behavior: merge
  literals:
  - BOOKIT_REGION=us-east

labels:
- pairs:
    bookit.io/region: us-east
  includeSelectors: false
