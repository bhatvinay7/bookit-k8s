apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - argocd-sealed-secrets.yaml
  - argocd-ingress.yaml

# Apply dev-apps.yaml or prod-apps.yaml explicitly from the branch-aware
# bootstrap workflow. Including both here could target the wrong environment.
